import { useControlScope } from "@/contexts/ControlScopeContext";
import { formatMoney, getLiveValidationGuidance } from "@/lib/control";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, ArrowRight, BadgeCheck, CheckCircle2, ChevronRight, CircleDollarSign, ClipboardCheck, Link2, ReceiptText, ShieldAlert } from "lucide-react";
import { Link } from "wouter";
import { StatusPill } from "@/components/StatusPill";
import { Skeleton } from "@/components/ui/skeleton";

function MetricCard({ label, value, note, icon: Icon, tone }: { label: string; value: string; note: string; icon: typeof CircleDollarSign; tone: string }) { return <section className="soft-card rise-in rounded-3xl border bg-card p-5"><div className="flex items-start justify-between"><p className="text-xs font-extrabold uppercase tracking-[0.13em] text-muted-foreground">{label}</p><div className={`grid size-9 place-items-center rounded-xl ${tone}`}><Icon className="size-[18px]" /></div></div><p className="money mt-5 text-2xl font-medium tracking-tight text-slate-900 sm:text-[1.7rem]">{value}</p><p className="mt-2 text-xs font-semibold text-muted-foreground">{note}</p></section>; }

function RunStep({ number, title, copy, complete, href }: { number: number; title: string; copy: string; complete: boolean; href: string }) {
  return <Link href={href} className="group flex items-center gap-3 rounded-2xl border border-transparent px-3 py-3 transition-colors hover:border-teal-100 hover:bg-teal-50/50"><div className={`grid size-8 shrink-0 place-items-center rounded-xl text-xs font-extrabold ${complete ? "bg-emerald-100 text-emerald-800" : "bg-slate-100 text-slate-600"}`}>{complete ? <CheckCircle2 className="size-4" /> : number}</div><div className="min-w-0 flex-1"><p className="text-sm font-extrabold">{title}</p><p className="mt-0.5 text-xs leading-5 text-muted-foreground">{copy}</p></div><ChevronRight className="size-4 text-muted-foreground transition-transform group-hover:translate-x-0.5 group-hover:text-teal-700" /></Link>;
}

export default function ControlDashboard() {
  const scope = useControlScope();
  const dashboard = trpc.control.dashboard.useQuery({ organisationId: scope.organisationId, branchId: scope.branchId });
  if (dashboard.isLoading) return <div className="space-y-6"><Skeleton className="h-28 rounded-3xl" /><div className="grid gap-4 md:grid-cols-3"><Skeleton className="h-36 rounded-3xl" /><Skeleton className="h-36 rounded-3xl" /><Skeleton className="h-36 rounded-3xl" /></div></div>;
  if (dashboard.isError) return <section className="rounded-3xl border border-rose-200 bg-rose-50 p-6"><ShieldAlert className="size-5 text-rose-700" /><h1 className="mt-3 font-extrabold">Control desk could not load</h1><p className="mt-1 text-sm text-rose-800">{dashboard.error.message}</p></section>;
  const data = dashboard.data!;
  const liveValidationGuidance = getLiveValidationGuidance(data.counts.recordedEvidence, data.counts.unresolvedExceptions);
  const nextAction = data.counts.unresolvedExceptions > 0
    ? { href: "/exceptions", label: "Review the priority exception", detail: "Resolve only the variance that needs accountable action." }
    : data.counts.obligations === 0
      ? { href: "/receivables", label: "Record the expected collection", detail: "Start with one customer and one receivable obligation." }
      : data.counts.recordedEvidence === 0
        ? { href: "/evidence", label: "Record independent evidence", detail: "Add a payment, settlement, or delivery observation with its source reference." }
        : { href: "/evidence", label: "Run the next match", detail: "Open the recorded evidence and apply the deterministic matching rule." };
  return <div className="mx-auto max-w-7xl space-y-6"><section className="rise-in overflow-hidden rounded-[1.8rem] bg-[#071d22] p-6 text-white sm:p-8"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[0.16em] text-teal-200">{scope.branchName} · Control desk</p><h1 className="mt-3 max-w-xl text-2xl font-extrabold tracking-tight sm:text-3xl">Decide from evidence, not assumptions.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">A scoped view of receivable position, recorded proof, deterministic matching, and operational exceptions.</p></div><div className="rounded-2xl border border-white/10 bg-white/6 px-4 py-3"><p className="text-[10px] font-extrabold uppercase tracking-[0.14em] text-teal-100/70">Reconciliation coverage</p><div className="mt-2 flex items-baseline gap-2"><span className="money text-2xl">{data.coverage.toFixed(2)}%</span><span className="text-xs text-slate-300">of in-scope position</span></div></div></div></section>
    <div className="grid gap-4 md:grid-cols-3"><MetricCard label="Receivable position" value={formatMoney(data.position.receivableMinor, data.position.currency)} note={`${data.counts.obligations} obligation${data.counts.obligations === 1 ? "" : "s"} in scope`} icon={CircleDollarSign} tone="bg-teal-50 text-teal-700" /><MetricCard label="Reconciled value" value={formatMoney(data.position.reconciledMinor, data.position.currency)} note={`${data.counts.recordedEvidence} recorded evidence event${data.counts.recordedEvidence === 1 ? "" : "s"}`} icon={Link2} tone="bg-sky-50 text-sky-700" /><MetricCard label="Needs attention" value={String(data.counts.unresolvedExceptions)} note="unresolved exception workload" icon={AlertTriangle} tone="bg-amber-50 text-amber-700" /></div>
    <section className="soft-card rounded-3xl border bg-card p-5 sm:p-6"><div className="flex flex-col gap-3 border-b pb-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-xl bg-teal-50 text-teal-700"><ClipboardCheck className="size-5" /></div><div><p className="text-sm font-extrabold">Today’s control run</p><p className="mt-1 text-xs text-muted-foreground">Four clear steps. The system keeps the trail; you focus only on the next decision.</p></div></div><Link href={nextAction.href} className="inline-flex items-center gap-1 text-xs font-extrabold text-teal-700 hover:text-teal-800">{nextAction.label} <ArrowRight className="size-3.5" /></Link></div><div className="mt-3 grid gap-2 lg:grid-cols-4"><RunStep number={1} title="Record expected value" copy="Capture the receivable once." complete={data.counts.obligations > 0} href="/receivables" /><RunStep number={2} title="Add independent proof" copy="Keep the source reference with it." complete={data.counts.recordedEvidence > 0} href="/evidence" /><RunStep number={3} title="Match deterministically" copy="Use the rule instead of a manual guess." complete={data.coverage > 0} href="/evidence" /><RunStep number={4} title="Resolve only variances" copy="No exception means no unnecessary work." complete={data.counts.unresolvedExceptions === 0 && data.counts.recordedEvidence > 0} href="/exceptions" /></div><p className="mt-4 rounded-xl bg-slate-50 px-3 py-2 text-xs leading-5 text-slate-600"><span className="font-extrabold text-slate-800">Next action:</span> {nextAction.detail}</p></section>
    <section className="rounded-3xl border border-teal-100 bg-teal-50/40 p-5 sm:p-6"><p className="text-xs font-extrabold uppercase tracking-[0.13em] text-teal-800">Built for the variations that matter</p><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-700">The daily flow stays the same even when the transaction is not. The system records the context and turns only the variance into review work.</p><div className="mt-4 grid gap-3 lg:grid-cols-2"><div className="rounded-2xl border border-emerald-200 bg-emerald-50/70 p-4"><p className="text-xs font-extrabold uppercase tracking-[0.12em] text-emerald-800">Implemented now</p><p className="mt-1 text-xs leading-5 text-emerald-900">Scoped access, exact money values, append-only history, split allocation limits, duplicate protection, delayed/unmatched exceptions, and approval-aware resolution are implemented and covered by automated checks.</p></div><div className="rounded-2xl border border-amber-200 bg-amber-50/70 p-4"><p className="text-xs font-extrabold uppercase tracking-[0.12em] text-amber-800">{liveValidationGuidance.title}</p><p className="mt-1 text-xs leading-5 text-amber-900">{liveValidationGuidance.message}</p></div></div><div className="mt-3 grid gap-3 md:grid-cols-2 xl:grid-cols-4"><div className="rounded-2xl border border-teal-100 bg-white/80 p-4"><p className="text-sm font-extrabold">Split collection</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Several payments can reduce one receivable without exceeding its remaining balance.</p></div><div className="rounded-2xl border border-teal-100 bg-white/80 p-4"><p className="text-sm font-extrabold">One payment, several items</p><p className="mt-1 text-xs leading-5 text-muted-foreground">The unallocated balance remains available for the next eligible obligation.</p></div><div className="rounded-2xl border border-teal-100 bg-white/80 p-4"><p className="text-sm font-extrabold">Delays and duplicates</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Late, repeated, excess, or unmatched proof is retained and routed to an exception.</p></div><div className="rounded-2xl border border-teal-100 bg-white/80 p-4"><p className="text-sm font-extrabold">People and branches</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Every action remains organisation- and branch-scoped with an accountable audit trail.</p></div></div></section>
    <div className="grid gap-6 xl:grid-cols-[1.5fr_1fr]"><section className="soft-card rounded-3xl border bg-card"><div className="flex items-center justify-between border-b px-5 py-4"><div><p className="text-sm font-extrabold">Priority exceptions</p><p className="mt-1 text-xs text-muted-foreground">Sorted by recorded urgency and status.</p></div><Link href="/exceptions" className="inline-flex items-center gap-1 text-xs font-extrabold text-teal-700 hover:text-teal-800">View all <ArrowRight className="size-3.5" /></Link></div>{data.priorityExceptions.length ? <div className="divide-y">{data.priorityExceptions.map(item => <div key={item.id} className="flex flex-col gap-3 px-5 py-4 sm:flex-row sm:items-center"><div className="grid size-9 shrink-0 place-items-center rounded-xl bg-amber-50 text-amber-700"><AlertTriangle className="size-[17px]" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-extrabold">{item.title}</p><p className="mt-1 text-xs text-muted-foreground">{item.type.replaceAll("_", " ")} · due {item.dueAt ? new Date(item.dueAt).toLocaleDateString() : "not set"}</p></div><div className="flex items-center gap-3 sm:text-right"><span className="money text-sm font-medium">{formatMoney(item.valueImpactMinor, item.currency ?? data.position.currency)}</span><StatusPill status={item.status} /></div></div>)}</div> : <div className="px-5 py-12 text-center"><BadgeCheck className="mx-auto size-7 text-emerald-600" /><p className="mt-3 text-sm font-extrabold">No priority exceptions yet</p><p className="mt-1 text-xs text-muted-foreground">Record evidence and run reconciliation to surface exceptions that need review.</p></div>}</section>
      <section className="soft-card rounded-3xl border bg-card p-5"><p className="text-xs font-extrabold uppercase tracking-[0.13em] text-muted-foreground">Control states</p><div className="mt-5 space-y-4"><div className="rounded-2xl bg-sky-50 p-4"><p className="text-sm font-extrabold text-sky-900">Recorded</p><p className="mt-1 text-xs leading-5 text-sky-800">Evidence exists with source provenance. It is not yet proof of settlement.</p></div><div className="rounded-2xl bg-teal-50 p-4"><p className="text-sm font-extrabold text-teal-900">Matched</p><p className="mt-1 text-xs leading-5 text-teal-800">A deterministic rule linked expected and observed records.</p></div><div className="rounded-2xl bg-amber-50 p-4"><p className="text-sm font-extrabold text-amber-900">Unresolved</p><p className="mt-1 text-xs leading-5 text-amber-800">A variance, delay, duplicate, or missing record still requires accountable action.</p></div></div><Link href="/receivables" className="mt-5 flex items-center justify-between rounded-2xl border px-4 py-3 text-sm font-extrabold hover:bg-slate-50">Review receivable records <ReceiptText className="size-4 text-teal-700" /></Link></section></div>
  </div>;
}
